# Videorc 0.9.107 release

Status: **prepared.** macOS `0.9.107-beta.1` is built from the protected-main
merge of this prep PR. Windows `0.9.107-alpha.1` is dispatched from protected
main after the macOS upload. This record is finalized after publication.

## Owner decisions (2026-09-24)

- **"merge it and release"** for #412 (light mode in every window), after
  which the owner merged #412 and this prep PR.
- macOS follows the regular Beta scope of 0.9.93 to 0.9.106: build, sign,
  notarize, staple and run the maintained artifact validation, then publish
  without the real-device, endurance (60/15-minute), clean-machine and strict
  provider gates. Those gates are recorded as not run; no PASS is claimed.
- **Neon is the only release origin** (owner, 2026-09-23).

## Scope and source

Since macOS 0.9.106 (`eea90897782c65763fec3f4d1bc5bb3a6947ce87`), protected
main gained:

- **#411**, the 0.9.106 changelog rewritten as a week ship note (docs only).
- **#412, light mode in every window.** Stream Manager, Notes and Captions
  were dark-always (plan 050): hard-coded `class="dark"` pages and a
  per-window `darkAqua` pin. They now follow the app theme through
  `window-theme-bootstrap.js` (prefers-color-scheme, driven by
  `nativeTheme.themeSource`), so text tokens and the vibrancy material agree
  and a toggle restyles open windows live. Only the Preview stays pinned
  dark. On Windows 11 the three windows get Mica like main (on-box check
  pending).

`0.9.106-alpha.1` was superseded before signing (see
[the 0.9.106 record](0.9.106.md)), so its unpublished changelog entry moves
forward as `0.9.107-alpha.1`, with the light-mode change added.

## Validation

- #412: typecheck, lint, format, desktop unit tests (2073), script tests and
  build pass locally; `probe:comments-window` PASS; `probe:ui-glass` 8/8 PASS
  across Stream Manager, Notes, Captions and Preview in dark and light, with
  the new `followsTheme` check and Preview `pinnedDark`. CI JS and Linux
  passed; merged by the owner before the Rust and Windows jobs reported.
