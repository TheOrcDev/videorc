# Videorc 0.9.101 release

Status: macOS `0.9.101-beta.1` is **published and verified** (2026-09-23 ~09:53Z).
Windows `0.9.101-alpha.1` candidate
[run 35845643459](https://github.com/TheOrcDev/videorc/actions/runs/35845643459)
is building from the same source; public Windows promotion waits for the owner
waiver. The coordinated release is partial until then.

## Published macOS identity

- Source: `14d8ffad408b783abeb9ea4b3cb7b55e2531edbc` (#391 squash on protected
  main, containing #389 and #390).
- Release: `0.9.101-beta.1`; numeric updater version `0.9.101`; macOS arm64.
- DMG: `Videorc-0.9.101-mac-arm64.dmg`, 159,306,839 bytes, SHA-256
  `b83d7f4b0cbeb8b977ef8a027348c619f728a06d2bb123c981cdc4fc85cc5270`.
- Updater ZIP: `Videorc-0.9.101-mac-arm64.zip`, 151,679,810 bytes, SHA-512
  (base64) `+BIpdNx8BPHze/YnnmV2DHIDdIJDcXG10E/KKG+fV2FYZzKE91W3qLG1ErAsro3l6sf8kOfySDoT8xtqSHexLg==`.
- Developer ID Application Uros Miric (`C2PA37RB58`): signing, notarization,
  stapling and `release:validate:macos` pass for the app and the DMG.
- Ordinary Beta publication (D3 record pending, not an exact promotion). All
  eight objects went to Hetzner (mirror) first and Neon (primary) last. The
  upload withheld `0.9.101-alpha.1`. No pending origin-sync records.

## Production verification (macOS)

- `/api/updates/latest-mac.yml` and `/api/updates/mirror/latest-mac.yml` serve
  `version: 0.9.101`. The full ZIP downloaded through each route matches the
  feed SHA-512 and size; both blockmaps return 200. Both routes resolve to Neon
  (mirror env is `none`), so independent mirror routing is not proven.
- `/releases/0.9.101-beta.1` returns 200; `/changelog` lists the Beta and not the
  withheld Alpha. Anonymous macOS download returns 401 as expected. Signed-in
  download-page acceptance was not performed in this session.
- Discord announcement for `0.9.101-beta.1` sent after verification.
- Gates not run, per the owner decision below: real-device/recording-studio
  bundle, 60/15-minute endurance sentinels, clean-machine installed acceptance,
  strict provider readiness, and #389's open S6 source-switch acceptance. No PASS
  is claimed for any of them.
- #390's post-merge CI completed green (JS, macOS Rust, Linux, Windows gates,
  Windows installer).

## Owner decisions (2026-09-23)

- **macOS: "just release it."** Build, sign, notarize, staple and run the
  maintained artifact validation, then publish without the real-device,
  endurance (60/15-minute), clean-machine and strict provider gates. Those gates
  are recorded below as not run; no PASS is claimed for them.
- **Windows: signed candidate plus an owner waiver.** After macOS is live, build
  and sign the `0.9.101-alpha.1` candidate from current protected main. Public
  promotion waits for the release owner to write and commit
  `docs/acceptance/windows-alpha/0.9.101-alpha.1.json` as an owner waiver
  (first public Alpha). The release agent does not author it.

## Scope and source

The release adds, on top of the originally prepared #386 scope:

- **#389, change capture sources during recording and streaming** (merged
  2026-09-23T09:22Z). Its CI passed (JS, macOS Rust, Linux, Windows gates and
  installer). Its PR checklist left final S6 acceptance open: maintained
  source-switch artifact/device scripts, both real pickers across
  recording/streaming/combined sessions, per-switch A/V timing and ten-minute
  repeated switching. Under the owner decision above those remain unverified for
  this release. Windows live window-source replacement is unsupported and is
  stated in the Alpha notes.
- **#390, Chat window stays whole at narrow widths.** Its local gates and
  `probe:comments-window` (167 assertions) passed; it merged before its CI
  finished, at owner request.

The original source note follows: the #386 scope started from protected main
`921903643ee43368058ffb93ec7d6328dcb224a0`, which includes the Upcoming workspace and scheduled YouTube event flow.
Public account connection remains paused until Google approval, so the event
flow is limited to authorized verification builds. Windows also carries
the 0.9.100 improvements that were never publicly released on that track.
The old `0.9.100-alpha.1` unsigned candidate run
[35771524986](https://github.com/TheOrcDev/videorc/actions/runs/35771524986)
passed its build checks but was cancelled before protected signing when this new
release was requested. It produced no signed installer or public release. Its
unpublished Alpha changelog entry was replaced with `0.9.101-alpha.1`; published
macOS 0.9.100 bytes and pointers remain untouched. The
[0.9.100 record](0.9.100.md) documents that macOS publication.

Windows candidate source commit: `14d8ffad408b783abeb9ea4b3cb7b55e2531edbc`
(main had not advanced after the macOS source). Signed installer identity, the
owner waiver and Windows announcement are pending.

## Required validation and known gaps

The scheduled-events implementation passed its local fixture, recording and RTMP
artifact smoke, typecheck, lint, build, desktop and Rust tests in #386. Real
YouTube channel create/edit/thumbnail/live/stop/cancel acceptance was not run
because the provider approval gate remains paused. This is not claimed as a
release-candidate PASS. The prior packaged macOS native-preview probe measured
24.2fps against its 30fps threshold; the new signed candidate must be retested.

The public Windows updater returns HTTP 404 because Windows has never been
publicly promoted, not because a pointer was lost. All 13
`promote-windows-alpha.yml` runs (2026-08-12 → 2026-09-21) used
`RELEASE_STAGE: pilot`. No acceptance record has ever been committed, so the
promotion guard's first-release path applies, and the older Windows changelog
entries came from macOS uploads publishing held entries. Public promotion of
`0.9.101-alpha.1` is therefore the first public Alpha
(`releaseSequence.kind: first-public-alpha`). See
[Plan 048](../../plans/048-windows-auto-update.md). Protected
Windows signing needs TheOrcDev's environment review. Public Windows promotion
requires exact-candidate physical and pilot acceptance or an owner-written,
owner-committed waiver, followed by production verification. No waiver is
prepared by the release agent.

Next action: validate the release branch, merge via reviewed PR, freeze the exact
macOS source, then build and test the signed candidate. Publish each platform
only after its required gate evidence or an explicit owner-directed exception.
No rollback has been performed and no secrets or private evidence are recorded
here.
