# Videorc 0.9.106 release

Status: macOS `0.9.106-beta.1` is **published and verified** (2026-09-24
~13:52Z). Windows `0.9.106-alpha.1` was **superseded before signing**: the
owner later asked for Windows, and candidate run
[36009846603](https://github.com/TheOrcDev/videorc/actions/runs/36009846603)
was dispatched from `d6183e23ca2c2f4bef5c70eccd4aa60701648a66`, but protected
main moved (#411, #412) before its sign job ran. Its unpublished changelog
entry moves forward as `0.9.107-alpha.1` (see [the 0.9.107 record](0.9.107.md)).

## Published macOS identity

- Source: `eea90897782c65763fec3f4d1bc5bb3a6947ce87` (#409 squash on protected
  main, on top of #408).
- Release: `0.9.106-beta.1`; numeric updater version `0.9.106`; macOS arm64.
- DMG: `Videorc-0.9.106-mac-arm64.dmg`, 163,995,429 bytes, SHA-256
  `9498187658276732a4429df2cc07011cbcd898b80a29821e53622f4a18248f96`.
- Updater ZIP: `Videorc-0.9.106-mac-arm64.zip`, 156,142,403 bytes, SHA-512
  (base64) `s/e6Pqz9kCwL5JafJIv9DsNOWYn+7OEX3H8uF/S2c0TKjb1N72hzZDcnka+FPiqIrwHkI7G4w8IyK74dxnQgWA==`.
- Developer ID Application Uros Miric (`C2PA37RB58`): signing, notarization,
  stapling and `release:validate:macos` pass for the app and the DMG.
- Built from the #409 branch head `ed1f3358` before the merge. Its tree
  `be46522cace76677e1fc9c3d7328bd26a3f37a06` is identical to the tree of
  `eea90897`, and the build embeds no commit hash, so the signed bytes are
  exactly this source.
- Ordinary Beta publication (D3 record pending, not an exact promotion). All
  eight objects went to **Neon only**; the upload withheld `0.9.106-alpha.1`.
  No pending origin-sync records.

## Production verification (macOS)

- `/api/updates/latest-mac.yml` and `/api/updates/mirror/latest-mac.yml` serve
  `version: 0.9.106`. The full ZIP downloaded through each route matches the
  feed SHA-512 and size; both blockmaps return 200. Both routes resolve to Neon.
- `/releases/0.9.106-beta.1` returns 200; `/api/changelog` lists 113 entries
  with `0.9.106-beta.1` newest and not the withheld Alpha. Signed-in
  download-page acceptance was not performed.
- Discord announcement for `0.9.106-beta.1` sent after verification.
- CI: #408 merged once its JS check passed (Rust, Linux and Windows jobs
  still running). #409 merged with JS and Linux green.

## Owner decisions (2026-09-24)

- **"Execute the entire plan, create a PR, and merge it as soon as possible
  and make a new macOS release,"** then **"merge it and release asap"** and
  **"I give you permission to merge"** for #409. macOS follows the regular
  Beta scope of 0.9.93 to 0.9.105: build, sign, notarize, staple and run the
  maintained artifact validation, then publish without the real-device,
  endurance (60/15-minute), clean-machine and strict provider gates. Those
  gates are recorded below as not run; no PASS is claimed for them.
- **macOS only.** No Windows candidate was dispatched for 0.9.106. The
  `0.9.106-alpha.1` changelog entry stays unpublished until a Windows
  candidate is built from protected main and accepted.
- **Neon is the only release origin** (owner, 2026-09-23).

## Scope and source

Since macOS 0.9.105 (`c2038a0b7187e659bad075afe28f9648deb8dd95`), protected
main gained:

- **#408, microphone dropouts since 0.9.103 (plan 056).** Every microphone
  take since #389 could drop packets that the session-audio bus rendered as
  exact-zero holes, worst with 128-frame CoreAudio callbacks (Shure MV7+).
  The bus now buffers one second in time instead of 32 packets, keeps
  ingesting the microphone while its FIFO write is blocked, and FFmpeg's
  audio input queue is 1024 packets again. Every loss is logged by reason
  when the bus closes, and the recording analyzer gained a digital-zero gate.

`0.9.105-alpha.1` was superseded before signing (see
[the 0.9.105 record](0.9.105.md)), so its unpublished changelog entry moves
forward as `0.9.106-alpha.1`, with the dropout fix added.

Mac source commit: `eea90897782c65763fec3f4d1bc5bb3a6947ce87`. Windows
candidate source commit: none (no candidate dispatched).

## Required validation and known gaps

- #408 passed its JS CI check, the targeted Rust tests (session audio,
  native audio, encoder-bridge argument tests), clippy, fmt and every script
  test locally; the real-FIFO bursty-reader test drops 0 frames where the
  0.9.105 bus dropped 38,144 of 66,080.
- `pnpm smoke:recording-studio` ran twice on the #408 tree. Every recording
  and audio stage passed; the captions-live smoke failed once on a transient
  admin-socket close (its backend log showed the bus with 0 dropped frames)
  and passed alone; the second bundle run stopped only at the detached
  preview lifecycle probe, a known failure on the owner's machine.
- Owed before this release is called complete: a 60 s Shure MV7+ take on the
  owner's box logging `0 frames dropped` in the `Session audio bus stopped`
  line, and the analyzer's digital-zero gate clean on it (plan 056 S4).
- Not run for this release, per the owner decision above: the real-device
  recording-studio bundle, the 60/15-minute endurance sentinels, clean-machine
  installed acceptance, and strict provider readiness.
