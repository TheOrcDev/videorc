# Videorc 0.9.108 release

Status: macOS `0.9.108-beta.1` is **published and verified** (2026-09-24
~20:39Z). Windows `0.9.108-alpha.1` is **not dispatched yet**: two stale
candidate runs hold the `windows-alpha-candidate` concurrency group, run
[36014186508](https://github.com/TheOrcDev/videorc/actions/runs/36014186508)
(from `c670cd2c`, waiting for sign approval) and run
[36031633371](https://github.com/TheOrcDev/videorc/actions/runs/36031633371)
(0.9.107, from `3f17f731`, pending behind it). The release agent was not
permitted to cancel them; the owner cancels them, then `0.9.108-alpha.1` is
dispatched from protected main.

## Published macOS identity

- Source: `fdc50b4d691d6781ceb54855ff36ad26d0b580e8` (#416 squash on protected
  main, on top of #415).
- Release: `0.9.108-beta.1`; numeric updater version `0.9.108`; macOS arm64.
- DMG: `Videorc-0.9.108-mac-arm64.dmg`, 163,997,206 bytes, SHA-256
  `bfce8ddc8225b9afa1445da5b82d69c218f7ca289c4b10f47b77a4b49bdf3dbd`.
- Updater ZIP: `Videorc-0.9.108-mac-arm64.zip`, 156,149,519 bytes, SHA-512
  (base64) `2xCf1i7bUHkslzDkA9NWYt1WsKX7D55LDSQF1d8e/TJ2MQV70NQgbXjd1mQruaqDz4RYfGGr4kmtik0LSrjmOw==`.
- Developer ID Application Uros Miric (`C2PA37RB58`): signing, notarization,
  stapling and `release:validate:macos` pass for the app and the DMG. The
  backend carries the bundled Twitch client ID and the X OAuth 1.0a consumer
  pair and no YouTube OAuth values, the same set as 0.9.107.
- Built from the #416 branch head `94484568`. Its tree
  `c95934316c83368be88c071eee62d20146203a47` is identical to the tree of
  `fdc50b4d`, and the build embeds no commit hash, so the signed bytes are
  exactly this source.
- Ordinary Beta publication (D3 record pending, not an exact promotion). All
  eight objects went to **Neon only**; the upload withheld `0.9.108-alpha.1`.
  No pending origin-sync records.

### A first build was rejected before upload

The first build loaded the release environment with a filter that matched
only `NAME=` lines, and the file writes the X OAuth 1.0a consumer pair as
`export NAME=`. Its backend was built without the pair (native X Live would
have asked for credentials), and `release:validate:macos` failed closed on
both values. Those bytes were set aside and never uploaded. The published
build loaded the environment with a filter that accepts the `export` prefix.

## Production verification (macOS)

- `/api/updates/latest-mac.yml` and the mirror route serve `0.9.108`.
- The zip returns 200 at 156,149,519 bytes and its SHA-512 matches the feed;
  the blockmap returns 200.
- `/releases/0.9.108-beta.1` returns 200; the anonymous download route
  returns 401 (account-gated, as designed).
- `/api/changelog` has 115 entries, including `0.9.108-beta.1`, and neither
  `0.9.108-alpha.1` nor `0.9.107-alpha.1`.
- The Discord announcement was sent.

## Owner decisions (2026-09-24)

- **"merge and release"** for #415 (plan 057, the Stream Manager puts chat
  first), and "The top left status badge should be all uppercase ON AIR",
  carried in this prep PR.
- macOS follows the regular Beta scope of 0.9.93 to 0.9.107: build, sign,
  notarize, staple and run the maintained artifact validation, then publish
  without the real-device, endurance (60/15-minute), clean-machine and strict
  provider gates. Those gates are recorded as not run; no PASS is claimed.
- **Neon is the only release origin** (owner, 2026-09-23).

## Scope and source

Since macOS 0.9.107 (`e3f4acb9fcfbe52a97ae98887a0871aed2c03cee`), protected
main gained:

- **#414**, the 0.9.106 week note moved into 0.9.107 (changelog only; it was
  published with 0.9.107).
- **#415, the Stream Manager puts chat first (plan 057).** The 128 px grid of
  stat tiles became one 32 px bar at every width. The clock, viewers and
  stream health lead it and are never clipped (health was missing below
  640 px). Health shows the bitrate when fine and names the problem
  otherwise. The other stats wrap out of sight whole when the window is
  narrow. Stats can be dragged into another order or hidden from a
  right-click menu; the clock and the viewer count stay in the first three
  slots. Text went quiet when fine: exception-only status and composer
  notes, hover-only chat times, one-line Activity facts (the stream's
  highlight card keeps the full sentence). Carried fixes: a pinned chat
  follows resizes, and Orcle's toast says Ctrl+J on Windows.
- **This PR:** the live chip reads ON AIR (owner call, 2026-09-24).

`0.9.107-alpha.1` was superseded before signing (see
[the 0.9.107 record](0.9.107.md)), so its unpublished changelog entry moves
forward as `0.9.108-alpha.1`, with the Stream Manager change added.

## Validation

- #415, locally: typecheck, lint (with the em-dash gate), format, desktop unit
  tests (2,094), script tests (1,467) and build pass.
  - `probe:comments-window` PASS (154 checks). The new checks: at 320 to
    1,280 px the stats bar is one row, the main three lead it whole and side
    by side, nothing is cut at its edge, and right-clicking a stat opens its
    menu.
  - `probe:ui-glass --gate` PASS for every window in dark and light.
  - `smoke:cohost-fake` PASS.
  - `check:renderer-assets`: the main window's eager JS is +95 bytes gzip
    against `origin/main` built on the same Mac.
- #415 in CI: JS (including the renderer asset budget), Linux, the Windows
  installer artifact and CodeRabbit passed. It was merged with the Rust and
  Windows gates still running; they are not required checks, and #415 changes
  no Rust.
- This PR: the ON AIR chip test (`comments-header.test.ts`) and
  `pnpm changelog:check` (116 entries). Every published changelog entry on
  main matches the live `/api/changelog` (114 entries); the only unpublished
  entry was `0.9.107-alpha.1`, which this PR moves forward.
