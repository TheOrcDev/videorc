# Videorc 0.9.103 release

Status: macOS `0.9.103-beta.1` is **published and verified** (2026-09-23
~22:50Z). Windows `0.9.103-alpha.1` **never signed**: protected main advanced
while its sign job waited for approval, so the job can no longer run. See
"Windows candidate" below. Windows continues as `0.9.104-alpha.1`; see the
[0.9.104 record](0.9.104.md).

## Published macOS identity

- Source: `a3ababf9460ce0a9c428d63a568eac7c92e41601` (#399 squash on protected
  main).
- Release: `0.9.103-beta.1`; numeric updater version `0.9.103`; macOS arm64.
- DMG: `Videorc-0.9.103-mac-arm64.dmg`, 159,442,792 bytes, SHA-256
  `86ecfed6ed77114e90a0f66094c9ef0a3db50574c5968d249e488d39a8daa137`.
- Updater ZIP: `Videorc-0.9.103-mac-arm64.zip`, 151,806,429 bytes, SHA-512
  (base64) `AuxETPBPPXjiYMerPzIEzSTe7lWA2piL6JtzTpqtRBLRRo5Etko6o9Xam87eMpAepMEUyRQ0JSsbmPvD0I3QOQ==`.
- Developer ID Application Uros Miric (`C2PA37RB58`): signing, notarization,
  stapling and `release:validate:macos` pass for the app and the DMG.
- Built from the #399 branch head `043d6dcf` before the merge. Its tree
  `516910e4d3bed614e15bac90cb1797c87170d148` is identical to the tree of
  `a3ababf9`, and the build embeds no commit hash, so the signed bytes are
  exactly this source.
- Ordinary Beta publication (D3 record pending, not an exact promotion), to
  **Neon only**; the upload withheld `0.9.103-alpha.1`.

## Production verification (macOS)

- `/api/updates/latest-mac.yml` and `/api/updates/mirror/latest-mac.yml` serve
  `version: 0.9.103`. The ZIP downloaded through the routes matches the feed
  SHA-512 and size; both blockmaps return 200.
- `/api/changelog` lists 110 entries with `0.9.103-beta.1` newest and not the
  withheld Alpha. Anonymous macOS download returns 401 as expected. Signed-in
  download-page acceptance was not performed.
- Discord announcement for `0.9.103-beta.1` sent after verification.
- CI: the owner said "dont wait ci just merge release", so #399 merged before
  its checks finished. Five of six passed. The macOS Rust job failed one
  timing-bounded unit test,
  `encoder_bridge::tests::videotoolbox_bridge_waits_bounded_for_fresh_compositor_sequence`
  (its publisher thread missed the 50 ms wait on the hosted runner; 2,285 other
  tests passed). The same suite passed on protected main in #402.

## Owner decisions (2026-09-24)

- **"Make a release."** macOS follows the regular Beta scope of 0.9.93 to
  0.9.102: build, sign, notarize, staple and run the maintained artifact
  validation, then publish without the real-device, endurance (60/15-minute),
  clean-machine and strict provider gates. Those gates are recorded below as not
  run; no PASS is claimed for them.
- **Neon is the only release origin** (owner, 2026-09-23).
- **Windows** builds the `0.9.103-alpha.1` candidate from protected main after
  macOS is live. The owner approves the protected sign job in GitHub: the
  auto-mode classifier treats a release agent approving its own dispatch as
  self-approval. Public promotion waits for the release owner to commit
  `docs/acceptance/windows-alpha/0.9.103-alpha.1.json`, either a PASS record or
  an owner waiver (first public Alpha).

## Scope and source

Since macOS 0.9.102 (`fdf6ce68743bf641a7db54619644d942dbbd7716`), protected
main gained:

- **#394, X scheduled broadcasts (plan 049).** X is the second provider of the
  Upcoming scheduler: schedule, edit, thumbnail, cancel, and Go Live through the
  saved broadcast's own ingest source.
- **#397, the app icon in About**, and in the Windows title row.
- **#398, X scheduling on the new design.** YouTube and X get separate Upcoming
  sections and forms. It also fixes the release-build break #394 introduced
  (`cargo build --release` failed on `X_SMOKE_ACCOUNT_ID`) and the Go Live
  preflight fixture gap that kept `smoke:scheduled-streams` from taking a saved
  X broadcast live; that smoke now passes for YouTube and X.

This prep also fixes the Windows signing blocker recorded in
[the 0.9.102 record](0.9.102.md): signed-bundle validation proves the
`ffmpeg-capture.exe` worker by its Authenticode signature (valid, exact
publisher, timestamped) instead of the pre-signing hash that signing always
changes. The unsigned bytes are still proven before signing.

`0.9.102-alpha.1` produced no stored candidate, so its unpublished changelog
entry moves forward as `0.9.103-alpha.1`.

Mac source commit: `a3ababf9460ce0a9c428d63a568eac7c92e41601`. Windows
candidate source commit: the same.

## Windows candidate

[Run 35930748475](https://github.com/TheOrcDev/videorc/actions/runs/35930748475)
built `0.9.103-alpha.1` from `a3ababf9` (the macOS source), dispatched
2026-09-23 22:53Z. The unsigned job passed. The protected sign job then waited
for the owner's approval while protected main advanced (#400, #401, #402), so it
can no longer sign: the job refuses a stale main. Nothing was signed, stored or
published, and the signed-bundle validation fix above is still proven only by
unit tests. The run was cancelled on 2026-09-24 before the `0.9.104-alpha.1`
candidate was dispatched, because it held the candidate concurrency group.

## Required validation and known gaps

- X scheduling has never run against a real X account: plan 049's S0 spike
  (create and delete one private schedule) and one real scheduled Go Live are
  still open. Only the fixture path is proven.
- Still pending from plan 050: the owner's by-eye checkpoints, Reduce
  Transparency and Increase Contrast in System Settings, a Windows 11 box pass
  (Mica, and now the title-row icon), and a `probe:preview-lifecycle` re-run on
  an idle machine.
- Not run for this release, per the owner decision above: the real-device
  recording-studio bundle, the 60/15-minute endurance sentinels, clean-machine
  installed acceptance, and strict provider readiness.
