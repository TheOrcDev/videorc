# Videorc 0.9.114 release

Status: macOS `0.9.114-beta.1` is **published and verified** (2026-09-25
~23:35Z). Windows `0.9.114-alpha.1` is dispatched from current `main` after
this record merges (see below). No Windows Alpha is public yet.

## Published macOS identity

- Source: `a4545090` (#453 squash on protected main, on top of #452). Built
  in the `release/0.9.114` worktree at exactly `a4545090` (tree
  `ad1b0ebf8559cab504dbdba91f38b074e2bd98ca`). `main` later gained the
  #437 Linux merge (`ef279218`), which is not in these bytes.
- Release: `0.9.114-beta.1`; numeric updater version `0.9.114`; macOS arm64.
- DMG: `Videorc-0.9.114-mac-arm64.dmg`, 164,252,859 bytes, SHA-256
  `59b3366988975ab5d7e586bc09727a1e08e4b8ccdfde935019744520121724f4`.
- Updater ZIP: `Videorc-0.9.114-mac-arm64.zip`, 156,382,499 bytes, SHA-512
  (base64) `fThwdSSdZxAdHABJVZYFZ/YjuJFmJZmn+cyMjjnFcpXkY82KlvJyTvXmquu1AhBStGZILCBCRJ0IYBipnd/oyQ==`.
- Developer ID Application Uros Miric (`C2PA37RB58`): signing, notarization,
  stapling and `release:validate:macos` pass (21 checks) for the app and the
  DMG; the bundled X OAuth 1.0a consumer pair is present, the Kick client
  secret is absent from `app.asar`, and the packaged backend contains the
  Kick client id and secret once each (counts only).
- Ordinary Beta publication. Uploaded to Hetzner (mirror) and Neon (primary);
  the upload withheld `0.9.114-alpha.1` (windows, linux) and
  `0.9.110-alpha.2` (linux).

## Production verification (macOS)

- `/api/updates/latest-mac.yml` and the mirror route serve `0.9.114`.
- The zip returns 200 at 156,382,499 bytes and its SHA-512 matches the feed;
  the blockmap returns 200.
- `/releases/0.9.114-beta.1` returns 200; `/api/downloads/macos/latest`
  returns 401 anonymously.
- `/api/changelog` has 121 entries, latest `0.9.114-beta.1`; no alpha leak.
- The Discord announcement was sent.

## Owner decisions (2026-09-26)

- "make a new release we added new pr for settings". A parallel session had
  merged #452 and #453 and built the artifacts; this session wrote the
  manifest, validated, uploaded, verified and announced them.
- macOS follows the regular Beta scope (build, sign, notarize, staple,
  artifact validation; device/endurance/clean-machine gates not run).

## Scope and source

Since macOS 0.9.113 (`90e56f36506eda3c01b85ca3d9ccbcbe31879331`):

- **#452, Settings in tabs (plan 064).** Seven tabs under the toolbar
  (General, Recording, Permissions, Shortcuts, Remote, Orcle, About); the
  strip stays pinned; Settings reopens on the last tab; ⌘K gets a Settings
  group; update, FFmpeg and shortcut messages open the tab that fixes them;
  "Animate scene changes" moves to General; the in-app reference becomes
  "App shortcuts".
- #451, #453: records and bump.

## Windows candidate

- `0.9.113-alpha.1` moved forward as `0.9.114-alpha.1` in #453.
- Remaining gates: `release-windows-alpha.yml` dispatched for
  `0.9.114-alpha.1` from current `main` after this record merges; the owner
  approves the protected sign job; acceptance or waiver record at
  `docs/acceptance/windows-alpha/0.9.114-alpha.1.json`; pilot and public
  promotion; web `VIDEORC_WINDOWS_ALPHA_STATE` flip. Nothing may merge to
  `main` between dispatch and sign approval, or the candidate goes stale.

## Follow-ups

- Kick live smoke on the owner's channel (`docs/oauth-live-smoke.md` Kick
  section, `docs/live-chat-live-smoke-checklist.md` Kick section) with this
  or a later build; confirm videorc-web migration 0015 is applied.
- Website "six platforms" sweep (plan 063 S7).
