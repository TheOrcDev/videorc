# Videorc 0.9.104 release

Status: macOS `0.9.104-beta.1` is **published and verified** (2026-09-24
~11:39Z). Windows `0.9.104-alpha.1` was **dropped before signing** by the
owner's decision: see "Windows candidate" below. Windows continues as
`0.9.105-alpha.1`; see the [0.9.105 record](0.9.105.md).

## Published macOS identity

- Source: `296ee36930f60b8dc9ff7c9252945c7c49c5979a` (#403 squash on protected
  main, on top of #402).
- Release: `0.9.104-beta.1`; numeric updater version `0.9.104`; macOS arm64.
- DMG: `Videorc-0.9.104-mac-arm64.dmg`, 163,981,317 bytes, SHA-256
  `ca407795a79525f3f849f0e8c5850adc49a4ed9c70e535e54eedce163beba636`.
- Updater ZIP: `Videorc-0.9.104-mac-arm64.zip`, 156,141,220 bytes, SHA-512
  (base64) `xRd+V1VRPzQlLacy2Uon85JcHfmoyrxfeW5RWWF+NIKXfcZFAaAvuM5P8qKgJRcJX9CroYf24wxbh9TfY6MlIg==`.
- Developer ID Application Uros Miric (`C2PA37RB58`): signing, notarization,
  stapling and `release:validate:macos` pass for the app and the DMG, including
  capture entitlements on every binary, the native preview addon signature and
  the bundled X consumer pair. The bundled Twitch client ID is present in the
  backend (checked by presence, never printed).
- Built from the #403 branch head `cad9739c` before the merge. Its tree
  `ee22126054770b92fb6170aad2441f68f79a14de` is identical to the tree of
  `296ee369`, and the build embeds no commit hash, so the signed bytes are
  exactly this source.
- Ordinary Beta publication (D3 record pending, not an exact promotion). All
  eight objects went to **Neon only**; the upload withheld `0.9.104-alpha.1`.
  No pending origin-sync records.

## Production verification (macOS)

- `/api/updates/latest-mac.yml` and `/api/updates/mirror/latest-mac.yml` serve
  `version: 0.9.104`. The full ZIP downloaded through each route matches the
  feed SHA-512 and size; both blockmaps return 200. Both routes resolve to Neon.
- `/releases/0.9.104-beta.1` returns 200; `/api/changelog` lists 111 entries
  with `0.9.104-beta.1` newest and not the withheld Alpha. Anonymous macOS
  download returns 401 as expected. Signed-in download-page acceptance was not
  performed.
- Discord announcement for `0.9.104-beta.1` sent after verification.
- CI: #402 passed all six checks on its final head. #403 (version, changelog
  and records only) merged once its JS check passed. Its Rust, Linux and
  Windows gates then passed too. Its Windows installer job built the
  installer, then failed the hosted-runner preview smoke on timing (preview
  source-to-present p99 1,221 ms against a 500 ms budget). #402 passed that
  smoke on identical code.

## Owner decisions (2026-09-24)

- **"Make a release,"** right after #402 merged. macOS follows the regular Beta
  scope of 0.9.93 to 0.9.103: build, sign, notarize, staple and run the
  maintained artifact validation, then publish without the real-device,
  endurance (60/15-minute), clean-machine and strict provider gates. Those gates
  are recorded below as not run; no PASS is claimed for them.
- **Neon is the only release origin** (owner, 2026-09-23).
- **Windows** builds the `0.9.104-alpha.1` candidate from protected main after
  macOS is live. The owner approves the protected sign job in GitHub, and
  nothing may merge to protected main while it waits: the job refuses a stale
  main, which is how `0.9.101-alpha.1` and `0.9.103-alpha.1` were lost. Public
  promotion waits for the release owner to commit
  `docs/acceptance/windows-alpha/0.9.104-alpha.1.json`, either a PASS record or
  an owner waiver (first public Alpha).
- **"Merge now, drop the candidate"** (12:13Z). The owner merged #405 before
  the `0.9.104-alpha.1` sign job could run, accepting that the candidate dies,
  then asked for another release: 0.9.105.

## Scope and source

Since macOS 0.9.103 (`a3ababf9460ce0a9c428d63a568eac7c92e41601`), protected
main gained:

- **#402, the Stream Manager (plan 055) and the X viewer count (plan 054).**
  The Chat window becomes a live dashboard: a stats strip (viewers, followers,
  supporters, tips, chat pace, stream health), an Activity feed, a richer chat
  and Orcle's own pane, laid out by container query from 320 px up. In the
  backend: one viewer total per session, structured event details, an audience
  poller, mid-stream token renewal, opt-in Twitch scopes, and an X viewer parser
  that reads the documented `data` envelope. Evidence:
  [the Stream Manager acceptance record](../acceptance/2026-09-24-stream-manager.md).
- **#400 and #401, Linux fixes (plans 052 and 053).** Linux-only apart from two
  shared changes. The owned-process registry now re-stamps the backend's
  identity when the backend reports ready. Windows software H.264 recordings
  now carry BT.709 colour tags, through the same `h264_metadata` rewrite the
  Media Foundation copy path already used. macOS encoder arguments are
  unchanged.

`0.9.103-alpha.1` never signed (see [the 0.9.103 record](0.9.103.md)), so its
unpublished changelog entry moves forward as `0.9.104-alpha.1`, with the Stream
Manager added.

Mac source commit: `296ee36930f60b8dc9ff7c9252945c7c49c5979a`. Windows
candidate source commit: the same.

## Windows candidate

[Run 35994373430](https://github.com/TheOrcDev/videorc/actions/runs/35994373430)
built `0.9.104-alpha.1` from `296ee369` (the macOS source), dispatched
2026-09-24 11:40Z after the stale `0.9.103-alpha.1` run was cancelled. While
its unsigned build and test job was still running, the owner chose to merge
#405 (the Stream Manager strip hairline) at once rather than wait for signing,
knowing the sign job refuses a moved main. The run was cancelled at 12:14Z.
Nothing was signed, stored or published. Windows continues as
`0.9.105-alpha.1`. The signed-bundle validation fix from the 0.9.103 prep is
still proven only by unit tests.

## Required validation and known gaps

- #402 passed all six CI checks on its final head and its local gates (see the
  acceptance record). Pending the owner:
  - one live stream to Twitch and X, where X must show a viewer count within
    60 s. That is plan 054's only real proof: the fix follows X's documented
    response and has not seen a live one;
  - follower counts checked against each platform;
  - Reconnect Twitch for follow alerts and the sub count;
  - a stream longer than four hours, for token renewal;
  - a 50 messages per second chat load check;
  - a by-eye review.
- Still open from earlier releases: plan 049's real-account X scheduling spike,
  and plan 050's by-eye and Windows 11 box checks.
- Not run for this release, per the owner decision above: the real-device
  recording-studio bundle, the 60/15-minute endurance sentinels, clean-machine
  installed acceptance, and strict provider readiness.
